import { NovaDiretivaDirective } from './nova-diretiva.directive';

describe('NovaDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new NovaDiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
